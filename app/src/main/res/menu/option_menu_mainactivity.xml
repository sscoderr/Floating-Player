<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/search_button"
        android:title="@string/search"
        android:icon="@mipmap/search_button"
        app:showAsAction="always"
        android:layout_width="wrap_content"
        android:orderInCategory="1"/>

    <item android:id="@+id/rate_button"
        android:title="@string/rateApp"
        app:showAsAction="never"
        android:layout_width="wrap_content" />
    <item android:id="@+id/share_button"
        android:title="@string/shareApp"
        app:showAsAction="never"
        android:layout_width="wrap_content" />

    <item android:id="@+id/checkHighQuality"
        android:checkable="true"
        android:title="@string/qualityOption"
        app:actionViewClass="android.widget.CheckBox"
        app:showAsAction="never|withText">
    </item>

    <item android:id="@+id/help_button"
        android:title="@string/help"
        app:showAsAction="never"
        android:layout_width="wrap_content" />

</menu>